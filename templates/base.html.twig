<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>{% block title %}Welcome!{% endblock %}</title>

    {% block stylesheets %}
        <link rel="stylesheet" href="https://bootswatch.com/4/flatly/bootstrap.min.css">
        <link rel="stylesheet" href="{{ asset("css/style.css") }}">
    {% endblock %}
</head>

<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
    <a class="navbar-brand" href="{{ path('home') }}">Musée du Louvre : Billeterie en ligne</a>

</nav>

<div class="container">
    <div class="row">
        {% for type, messages in app.flashes %}
            {% for message in messages %}
                <div class="col-8 offset-2 alert alert-{{ type }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>

    {% block body %}{% endblock %}


    <nav class="row bg-primary mt-5 fixed-bottom">
        <div class="col-lg-6">
            <p class="text-muted text-center">
                Site conçu avec bootstrap et développé avec Symfony
            </p>
        </div>
        <div class="col-lg-6">
            <p class="text-muted text-center">
                Developpement et Design : Bayram KILIC. (Dans le cadre d'un projet OpenClassrooms)
            </p>
        </div>
    </nav>

</div>

{% block javascripts %}{% endblock %}

</body>
</html>