{% extends 'base.html.twig' %}

{% block title %}Achat de billets en ligne{% endblock %}

{% block body %}

    <h4> Etape 1 : Choix des billets</h4>

    {{ form_start(formStepOne) }}
    {{ form_widget(formStepOne) }}
    <button type="submit" class="btn btn-success">Valider</button>
    {{ form_end(formStepOne) }}



{% endblock %}
